# 🏁 How to set up your DVKK OS Profile

I've created the files for you. To make this your GitHub profile:

1.  **Create a New Repo** on GitHub named exactly `diiviikk5` (it must match your username).
2.  **Initialize & Push**:
    Open your terminal in `C:\Users\Divik\diiviikk5` and run:
    ```bash
    git remote add origin https://github.com/diiviikk5/diiviikk5.git
    git add .
    git commit -m "feat: initial system boot (DVKK OS)"
    git branch -M main
    git push -u origin main
    ```
3.  **Activate the Snake**:
    After you push, the "Snake" contribution graph will start generating automatically. 
    *   Go to the **Actions** tab in your `diiviikk5` repo.
    *   Select **generate animation**.
    *   Click **Run workflow**.
    *   This will create an `output` branch where the image lives.

4.  **Show Private Contributions**:
    To make sure your "greens" from private repos (like Drift) show up:
    *   Go to your profile page.
    *   Click the dropdown at the top right of your contribution graph.
    *   Select **Private contributions**.

Your profile is now a 1995 DOS Racing terminal! 🏎️💨
